PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=librava_db
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/librava_db?schema=public"

# JWT Configuration
JWT_SECRET=librava_secret_jwt_key_2026_super_secure
JWT_EXPIRES_IN=7d

